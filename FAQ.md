# ❓ Frequently Asked Questions (FAQ)
## الأسئلة الشائعة

---

## 🔧 مشاكل التثبيت - Installation Issues

### Q1: لا أستطيع تثبيت psycopg2
**السؤال**: عند تشغيل `pip install -r backend/requirements.txt` أحصل على خطأ في تثبيت psycopg2

**الحل**:
```bash
# استخدم psycopg2-binary بدلاً من psycopg2
pip install psycopg2-binary
```

أو قم بتحديث `backend/requirements.txt`:
```
psycopg2-binary  # بدلاً من psycopg2
```

---

### Q2: Python لا يعمل في Command Prompt
**السؤال**: عند كتابة `python` أحصل على خطأ "command not found"

**الحل**:
1. تأكد من تثبيت Python
2. أعد تثبيت Python وتأكد من تفعيل "Add Python to PATH"
3. أو استخدم المسار الكامل:
```bash
C:\Users\YourName\AppData\Local\Programs\Python\Python3X\python.exe
```

---

### Q3: npm لا يعمل
**السؤال**: عند كتابة `npm` أحصل على خطأ

**الحل**:
1. تأكد من تثبيت Node.js
2. أعد تشغيل Command Prompt بعد التثبيت
3. تحقق من التثبيت:
```bash
node --version
npm --version
```

---

## 🗄️ مشاكل قاعدة البيانات - Database Issues

### Q4: لا أستطيع الاتصال بقاعدة البيانات
**السؤال**: أحصل على خطأ "could not connect to server"

**الحل**:
1. تأكد من تشغيل PostgreSQL:
   - Windows: افتح Services وابحث عن postgresql
   - Linux: `sudo systemctl status postgresql`
   - Mac: `brew services list`

2. تحقق من كلمة المرور في `backend/.env`
3. تأكد من اسم قاعدة البيانات `studentdb`

---

### Q5: قاعدة البيانات موجودة لكن الجداول فارغة
**السؤال**: قاعدة البيانات موجودة لكن لا توجد جداول

**الحل**:
الجداول تُنشأ تلقائياً عند تشغيل `python run.py` لأول مرة.

إذا لم تُنشأ، قم بإنشائها يدوياً:
```python
python
>>> from backend.app import create_app, db
>>> app = create_app()
>>> with app.app_context():
...     db.create_all()
>>> exit()
```

---

### Q6: كيف أغير كلمة مرور PostgreSQL؟
**السؤال**: نسيت كلمة مرور PostgreSQL

**الحل**:
```bash
# الدخول كمستخدم postgres
psql -U postgres

# تغيير كلمة المرور
ALTER USER postgres PASSWORD 'new_password';
```

---

## 🚀 مشاكل التشغيل - Runtime Issues

### Q7: Port 5000 already in use
**السؤال**: أحصل على خطأ "Port 5000 is already in use"

**الحل**:
**الخيار 1**: أغلق البرنامج الذي يستخدم المنفذ 5000

**الخيار 2**: غيّر المنفذ في `backend/run.py`:
```python
app.run(debug=True, port=5001)  # استخدم منفذ مختلف
```

---

### Q8: CORS error في المتصفح
**السؤال**: أحصل على خطأ CORS في console المتصفح

**الحل**:
```bash
# تأكد من تثبيت flask-cors
pip install flask-cors
```

تحقق من وجود هذا السطر في `backend/app.py`:
```python
from flask_cors import CORS
CORS(app)
```

---

### Q9: Frontend لا يتصل بـ Backend
**السؤال**: Frontend يعمل لكن لا توجد بيانات

**الحل**:
1. تأكد من تشغيل Backend على `http://127.0.0.1:5000`
2. تحقق من عنوان API في كود Frontend
3. افتح Developer Tools في المتصفح وتحقق من Network tab

---

## 👤 مشاكل تسجيل الدخول - Login Issues

### Q10: لا أستطيع تسجيل الدخول بحساب الأدمن
**السؤال**: البريد وكلمة المرور الافتراضية لا تعمل

**الحل**:
تحقق من إنشاء حساب الأدمن:
```bash
# في PostgreSQL
psql -U postgres studentdb
SELECT * FROM users WHERE email = 'admin@admin.com';
```

إذا لم يكن موجوداً، أنشئه يدوياً:
```sql
INSERT INTO users (id, name, email, password, role)
VALUES ('1', 'admin', 'admin@admin.com', 'admin', 'ADMIN');
```

---

### Q11: كيف أغير كلمة مرور الأدمن؟
**السؤال**: أريد تغيير كلمة المرور الافتراضية

**الحل**:
```sql
-- في PostgreSQL
psql -U postgres studentdb
UPDATE users SET password = 'new_password' WHERE email = 'admin@admin.com';
```

**ملاحظة**: في نظام حقيقي، يجب تشفير كلمة المرور!

---

## 📁 مشاكل الملفات - File Issues

### Q12: لا أجد ملف .env
**السؤال**: لا يوجد ملف `.env` في مجلد backend

**الحل**:
يجب إنشاؤه يدوياً! استخدم `backend/.env.example` كمثال:

```bash
# في مجلد backend
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

ثم عدّل محتوى `.env`:
```env
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/studentdb
```

---

### Q13: الملفات المخفية لا تظهر
**السؤال**: لا أرى ملفات مثل `.env` أو `.gitignore`

**الحل**:
**Windows**:
1. افتح File Explorer
2. اذهب إلى View > Show > Hidden items

**Mac**:
```bash
# في Finder
Command + Shift + . (نقطة)
```

**Linux**:
```bash
# في Terminal
ls -la
```

---

## 🔄 مشاكل التحديث - Update Issues

### Q14: كيف أحدّث المكتبات؟
**السؤال**: أريد تحديث المكتبات لأحدث إصدار

**الحل**:
**Backend**:
```bash
pip install --upgrade -r backend/requirements.txt
```

**Frontend**:
```bash
npm update
```

---

### Q15: أضفت مكتبة جديدة، كيف أحفظها؟
**السؤال**: أضفت مكتبة جديدة وأريد إضافتها للمشروع

**الحل**:
**Backend**:
```bash
pip freeze > backend/requirements.txt
```

**Frontend**:
```bash
npm install <package-name> --save
```

---

## 🧹 مشاكل التنظيف - Cleanup Issues

### Q16: كيف أحذف قاعدة البيانات وأبدأ من جديد؟
**السؤال**: أريد مسح كل شيء والبدء من الصفر

**الحل**:
```bash
# حذف قاعدة البيانات
psql -U postgres
DROP DATABASE studentdb;
CREATE DATABASE studentdb;
\q

# ثم شغّل Backend من جديد
cd backend
python run.py
```

---

### Q17: كيف أحذف البيانات فقط وليس الجداول؟
**السؤال**: أريد مسح البيانات لكن الاحتفاظ ببنية الجداول

**الحل**:
```sql
-- في PostgreSQL
psql -U postgres studentdb

-- حذف جميع البيانات
TRUNCATE users, students, universities, programs, applications, application_messages, notifications CASCADE;

-- إعادة إنشاء حساب الأدمن
INSERT INTO users (id, name, email, password, role)
VALUES ('1', 'admin', 'admin@admin.com', 'admin', 'ADMIN');
```

---

## 🌐 مشاكل الشبكة - Network Issues

### Q18: كيف أجعل التطبيق متاحاً على الشبكة المحلية؟
**السؤال**: أريد الوصول للتطبيق من جهاز آخر على نفس الشبكة

**الحل**:
**Backend** - عدّل `backend/run.py`:
```python
app.run(debug=True, host='0.0.0.0', port=5000)
```

**Frontend** - عدّل `vite.config.ts`:
```typescript
export default defineConfig({
  server: {
    host: '0.0.0.0',
    port: 5173
  }
})
```

ثم استخدم IP الجهاز:
```
http://192.168.1.X:5173
```

---

## 📦 مشاكل النشر - Deployment Issues

### Q19: كيف أنشر التطبيق على الإنترنت؟
**السؤال**: أريد جعل التطبيق متاحاً على الإنترنت

**الحل**:
هناك عدة خيارات:

**Backend**:
- Heroku (مجاني)
- Railway (مجاني)
- Render (مجاني)
- DigitalOcean
- AWS

**Frontend**:
- Vercel (مجاني)
- Netlify (مجاني)
- GitHub Pages

**قاعدة البيانات**:
- ElephantSQL (PostgreSQL مجاني)
- Supabase (PostgreSQL مجاني)
- Heroku Postgres

---

### Q20: كيف أنشئ ملف executable للتطبيق؟
**السؤال**: أريد تحويل التطبيق لملف .exe

**الحل**:
**Backend**:
```bash
pip install pyinstaller
pyinstaller --onefile backend/run.py
```

**Frontend**:
```bash
npm run build
# ثم استخدم Electron لتحويله لتطبيق desktop
```

---

## 💡 نصائح عامة - General Tips

### Q21: ما هي أفضل الممارسات للأمان؟
**الإجابة**:
1. **لا تستخدم** كلمة المرور الافتراضية في الإنتاج
2. استخدم تشفير لكلمات المرور (bcrypt)
3. استخدم HTTPS في الإنتاج
4. لا ترفع ملف `.env` على Git
5. استخدم JWT للمصادقة
6. قم بتحديث المكتبات بانتظام

---

### Q22: كيف أضيف ميزات جديدة؟
**الإجابة**:
1. أضف Model في `backend/models.py`
2. أضف Routes في `backend/routes.py`
3. أضف Component في `components/`
4. حدّث `App.tsx` إذا لزم الأمر
5. اختبر الميزة الجديدة

---

### Q23: كيف أحصل على الدعم؟
**الإجابة**:
1. راجع `SETUP_GUIDE.md`
2. راجع `DATABASE_SCHEMA.md`
3. تحقق من رسائل الأخطاء في Terminal
4. ابحث عن الخطأ في Google
5. اسأل في Stack Overflow

---

## 📞 لم تجد إجابة؟

إذا لم تجد إجابة لسؤالك:
1. راجع الملفات التوثيقية الأخرى
2. تحقق من رسائل الأخطاء بعناية
3. ابحث عن الخطأ على الإنترنت
4. اطلب المساعدة من مطور آخر

---

**آخر تحديث**: فبراير 2026
