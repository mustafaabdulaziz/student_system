# ✅ Deployment Checklist
## قائمة التحقق قبل إرسال المشروع

استخدم هذه القائمة للتأكد من أن المشروع جاهز للإرسال لشخص آخر.

---

## 📦 قبل الإرسال - Before Sending

### 1. تنظيف الملفات الكبيرة
- [ ] حذف مجلد `.venv` (سيتم إنشاؤه من جديد)
  ```bash
  # Windows
  rmdir /s /q .venv
  
  # Linux/Mac
  rm -rf .venv
  ```

- [ ] حذف مجلد `node_modules` (سيتم إنشاؤه من جديد)
  ```bash
  # Windows
  rmdir /s /q node_modules
  
  # Linux/Mac
  rm -rf node_modules
  ```

- [ ] حذف مجلد `__pycache__` إن وُجد
  ```bash
  # Windows
  for /d /r . %d in (__pycache__) do @if exist "%d" rd /s /q "%d"
  
  # Linux/Mac
  find . -type d -name __pycache__ -exec rm -rf {} +
  ```

### 2. حماية المعلومات الحساسة
- [ ] حذف ملف `backend/.env` (يحتوي على معلومات حساسة)
- [ ] التأكد من وجود `backend/.env.example` (كمثال للمستخدم الجديد)
- [ ] مراجعة ملف `.gitignore` للتأكد من عدم رفع ملفات حساسة

### 3. التأكد من وجود الملفات المطلوبة
- [ ] `backend/requirements.txt` موجود ومحدّث
- [ ] `package.json` موجود ومحدّث
- [ ] `README.md` موجود
- [ ] `SETUP_GUIDE.md` موجود
- [ ] `DATABASE_SCHEMA.md` موجود
- [ ] `backend/.env.example` موجود
- [ ] `setup_database.bat` موجود (لمستخدمي Windows)
- [ ] `setup_database.sh` موجود (لمستخدمي Linux/Mac)

### 4. مراجعة الكود
- [ ] لا توجد كلمات مرور مكشوفة في الكود
- [ ] لا توجد API keys مكشوفة
- [ ] لا توجد معلومات شخصية في الكود

---

## 📝 ملفات التوثيق المطلوبة

تأكد من وجود هذه الملفات:

| الملف | الوصف | ✅ |
|------|-------|---|
| `README.md` | دليل البدء السريع | [ ] |
| `SETUP_GUIDE.md` | دليل الإعداد الكامل | [ ] |
| `DATABASE_SCHEMA.md` | توثيق بنية قاعدة البيانات | [ ] |
| `DEPLOYMENT_CHECKLIST.md` | هذا الملف | [ ] |
| `backend/.env.example` | مثال لملف الإعدادات | [ ] |
| `backend/requirements.txt` | مكتبات Python | [ ] |
| `package.json` | مكتبات Node.js | [ ] |

---

## 🔍 اختبار سريع قبل الإرسال

### اختبار على جهازك
- [ ] قم بحذف `.venv` و `node_modules`
- [ ] اتبع خطوات `SETUP_GUIDE.md` من البداية
- [ ] تأكد من أن Backend يعمل بدون أخطاء
- [ ] تأكد من أن Frontend يعمل بدون أخطاء
- [ ] تأكد من إمكانية تسجيل الدخول بحساب الأدمن

---

## 📤 طرق الإرسال

### الطريقة 1: ملف مضغوط (ZIP)
```bash
# ضغط المشروع (بعد حذف .venv و node_modules)
# Windows: استخدم "Send to > Compressed folder"
# Linux/Mac:
zip -r student-system.zip student-system/ -x "*.venv/*" "*/node_modules/*" "*/__pycache__/*" "*/backend/.env"
```

### الطريقة 2: Git Repository
```bash
# إنشاء repository جديد
git init
git add .
git commit -m "Initial commit"

# رفع على GitHub/GitLab
git remote add origin <repository-url>
git push -u origin main
```

---

## 📊 حجم المشروع المتوقع

| المجلد/الملف | الحجم التقريبي |
|-------------|----------------|
| المشروع الكامل (مع node_modules و .venv) | ~500 MB |
| المشروع بدون node_modules و .venv | ~5-10 MB |

**💡 نصيحة**: أرسل المشروع بدون `node_modules` و `.venv` لتقليل الحجم!

---

## 📧 معلومات للمستلم

عند إرسال المشروع، أرسل هذه المعلومات:

```
مرحباً،

إليك مشروع نظام إدارة الطلاب (Student System).

📖 للبدء:
1. افتح ملف README.md للبدء السريع
2. راجع SETUP_GUIDE.md للتعليمات الكاملة
3. راجع DATABASE_SCHEMA.md لفهم بنية قاعدة البيانات

🔑 معلومات تسجيل الدخول الافتراضية:
- Email: admin@admin.com
- Password: admin

⚠️ ملاحظات مهمة:
- يجب تثبيت PostgreSQL قبل التشغيل
- يجب إنشاء ملف backend/.env (راجع backend/.env.example)
- يُنصح بتغيير كلمة مرور الأدمن بعد أول تسجيل دخول

📞 للدعم:
[أضف معلومات الاتصال الخاصة بك هنا]
```

---

## ✅ القائمة النهائية

قبل الإرسال، تأكد من:

- [ ] حذف `.venv`
- [ ] حذف `node_modules`
- [ ] حذف `backend/.env`
- [ ] حذف `__pycache__`
- [ ] وجود جميع ملفات التوثيق
- [ ] اختبار الإعداد من الصفر
- [ ] ضغط المشروع أو رفعه على Git
- [ ] إرسال التعليمات للمستلم

---

## 🎉 جاهز للإرسال!

إذا أكملت جميع النقاط أعلاه، المشروع جاهز للإرسال! 🚀
