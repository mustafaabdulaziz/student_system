# دليل تشغيل نظام إدارة الطلاب
## Student System Setup Guide

هذا الدليل يشرح كيفية تشغيل المشروع من الصفر على جهاز جديد.

---

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت البرامج التالية:

### 1. Python (الإصدار 3.8 أو أحدث)
- تحميل من: https://www.python.org/downloads/
- **مهم**: أثناء التثبيت، تأكد من تفعيل خيار "Add Python to PATH"

### 2. Node.js (الإصدار 16 أو أحدث)
- تحميل من: https://nodejs.org/
- يتضمن npm تلقائياً

### 3. PostgreSQL (الإصدار 12 أو أحدث)
- تحميل من: https://www.postgresql.org/download/
- **مهم جداً**: احفظ كلمة المرور التي تختارها للمستخدم `postgres` أثناء التثبيت

---

## 🗄️ إعداد قاعدة البيانات

### الخطوة 1: التأكد من تشغيل PostgreSQL

**على Windows:**
1. افتح "Services" (ابحث عن services.msc)
2. ابحث عن "postgresql-x64-XX" (حيث XX هو رقم الإصدار)
3. تأكد أن الحالة "Running"
4. إذا لم تكن تعمل، اضغط بزر الماوس الأيمن واختر "Start"

### الخطوة 2: إنشاء قاعدة البيانات

افتح Command Prompt أو PowerShell وقم بتنفيذ:

```bash
# الدخول إلى PostgreSQL
psql -U postgres

# داخل PostgreSQL، قم بإنشاء قاعدة البيانات
CREATE DATABASE studentdb;

# للخروج من PostgreSQL
\q
```

**ملاحظة**: إذا طُلب منك كلمة المرور، أدخل كلمة المرور التي اخترتها عند تثبيت PostgreSQL.

### الخطوة 3: التحقق من إنشاء قاعدة البيانات

```bash
psql -U postgres -l
```

يجب أن ترى `studentdb` في قائمة قواعد البيانات.

---

## ⚙️ إعداد المشروع

### 1. نسخ المشروع

إذا استلمت المشروع كملف مضغوط:
```bash
# فك الضغط إلى مجلد معين
# ثم انتقل إلى مجلد المشروع
cd path/to/student-system
```

### 2. إعداد Backend (Python/Flask)

#### أ. إنشاء بيئة افتراضية

```bash
# في مجلد المشروع الرئيسي
python -m venv .venv
```

#### ب. تفعيل البيئة الافتراضية

**على Windows (PowerShell):**
```powershell
.\.venv\Scripts\Activate.ps1
```

**على Windows (Command Prompt):**
```cmd
.venv\Scripts\activate.bat
```

**على Linux/Mac:**
```bash
source .venv/bin/activate
```

#### ج. تثبيت المكتبات المطلوبة

```bash
pip install -r backend/requirements.txt
```

#### د. إضافة Flask-CORS (مطلوب للمشروع)

```bash
pip install flask-cors
```

### 3. إعداد Frontend (React/Vite)

```bash
# تثبيت المكتبات
npm install
```

### 4. إعداد ملف البيئة (Environment Variables)

المشروع يحتوي على ملف `.env.local` لكن يجب إضافة معلومات قاعدة البيانات:

#### أ. إنشاء ملف `.env` في مجلد `backend`

أنشئ ملف جديد باسم `.env` في مجلد `backend` وأضف المحتوى التالي:

```env
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/studentdb
```

**استبدل `YOUR_PASSWORD` بكلمة مرور PostgreSQL الخاصة بك**

مثال:
```env
DATABASE_URL=postgresql://postgres:mypassword123@localhost:5432/studentdb
```

#### ب. تحديث `.env.local` (اختياري)

إذا كنت تريد استخدام Gemini AI للمحادثات، قم بتحديث الملف `.env.local`:

```env
GEMINI_API_KEY=your_actual_api_key_here
```

---

## 🚀 تشغيل المشروع

يجب تشغيل Backend و Frontend معاً في نافذتين منفصلتين.

### 1. تشغيل Backend

افتح نافذة Terminal/Command Prompt:

```bash
# تأكد من تفعيل البيئة الافتراضية
.\.venv\Scripts\Activate.ps1  # على Windows PowerShell

# انتقل إلى مجلد backend
cd backend

# شغّل السيرفر
python run.py
```

**يجب أن ترى:**
```
* Running on http://127.0.0.1:5000
* Restarting with stat
* Debugger is active!
```

### 2. تشغيل Frontend

افتح نافذة Terminal/Command Prompt **جديدة**:

```bash
# في مجلد المشروع الرئيسي
npm run dev
```

**يجب أن ترى:**
```
VITE v6.x.x  ready in XXX ms

➜  Local:   http://localhost:5173/
```

### 3. فتح التطبيق

افتح المتصفح واذهب إلى:
```
http://localhost:5173
```

---

## 👤 تسجيل الدخول الأول

عند تشغيل Backend لأول مرة، يتم إنشاء حساب أدمن تلقائياً:

- **البريد الإلكتروني**: `admin@admin.com`
- **كلمة المرور**: `admin`

**⚠️ مهم**: يُنصح بتغيير كلمة المرور بعد أول تسجيل دخول!

---

## 🔧 حل المشاكل الشائعة

### مشكلة: "psycopg2 not found"

```bash
pip install psycopg2-binary
```

### مشكلة: "Cannot connect to database"

1. تأكد من تشغيل PostgreSQL
2. تحقق من كلمة المرور في ملف `.env`
3. تأكد من اسم قاعدة البيانات `studentdb`

### مشكلة: "Port 5000 already in use"

إذا كان المنفذ 5000 مستخدماً:
1. أغلق البرنامج الذي يستخدمه
2. أو غيّر المنفذ في `run.py`:
```python
app.run(debug=True, port=5001)
```

### مشكلة: "CORS error" في المتصفح

تأكد من تثبيت `flask-cors`:
```bash
pip install flask-cors
```

### مشكلة: الجداول غير موجودة في قاعدة البيانات

البرنامج يقوم بإنشاء الجداول تلقائياً عند التشغيل الأول. إذا لم تُنشأ:

```bash
# في Python shell
python
>>> from backend.app import create_app, db
>>> app = create_app()
>>> with app.app_context():
...     db.create_all()
>>> exit()
```

---

## 📦 ملفات المشروع المهمة

```
student-system/
├── backend/
│   ├── app.py              # إعداد Flask
│   ├── models.py           # نماذج قاعدة البيانات
│   ├── routes.py           # API endpoints
│   ├── run.py              # ملف التشغيل الرئيسي
│   ├── requirements.txt    # مكتبات Python
│   └── .env               # إعدادات قاعدة البيانات (يجب إنشاؤه)
├── components/            # مكونات React
├── App.tsx               # التطبيق الرئيسي
├── package.json          # مكتبات Node.js
├── .env.local           # إعدادات Frontend
└── .venv/               # البيئة الافتراضية (تُنشأ تلقائياً)
```

---

## 📝 ملاحظات إضافية

### إيقاف التطبيق

- **Backend**: اضغط `Ctrl + C` في نافذة Terminal
- **Frontend**: اضغط `Ctrl + C` في نافذة Terminal

### تحديث المكتبات

إذا أضفت مكتبات جديدة:

**Backend:**
```bash
pip freeze > backend/requirements.txt
```

**Frontend:**
```bash
npm install
```

### النسخ الاحتياطي لقاعدة البيانات

```bash
pg_dump -U postgres studentdb > backup.sql
```

### استعادة قاعدة البيانات

```bash
psql -U postgres studentdb < backup.sql
```

---

## 🆘 الدعم

إذا واجهت أي مشاكل:
1. تحقق من أن جميع المتطلبات مثبتة بشكل صحيح
2. تأكد من تشغيل PostgreSQL
3. راجع رسائل الأخطاء في Terminal
4. تحقق من ملف `.env` في مجلد backend

---

## ✅ قائمة التحقق السريعة

قبل إرسال المشروع لشخص آخر، تأكد من:

- [ ] حذف مجلد `.venv` (سيتم إنشاؤه من جديد)
- [ ] حذف مجلد `node_modules` (سيتم إنشاؤه من جديد)
- [ ] حذف ملف `.env` من backend (يحتوي على معلومات حساسة)
- [ ] التأكد من وجود `requirements.txt`
- [ ] التأكد من وجود `package.json`
- [ ] إضافة هذا الملف `SETUP_GUIDE.md`

**حجم المشروع بعد الحذف سيكون أصغر بكثير وأسهل للنقل!**
